export const metadata = {
  title: 'Showcase - See Mues AI in action',
  description: "See how Mues AI takes action inside popular web apps to drive feature adoption and reduce user friction.",
};

export default function ShowcaseLayout({ children }) {
  return children;
} 