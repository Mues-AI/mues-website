export const metadata = {
  title: "ROI Calculator | Mues AI",
  description: "Your users delegate any task to an AI agent using human language in your product.",
};

export default function RoiCalculatorLayout({ children }) {
  return children;
} 
