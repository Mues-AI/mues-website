export const metadata = {
  title: 'AI-Powered Onboarding - Use Case',
  description: "Mues AI offers personalized onboarding tailored to each user's needs, helping them recognize value early by providing instant answers and eliminating learning curves.",
};

export default function OnboardingLayout({ children }) {
  return children;
} 